//! Shared surface for the showcase binaries (`feotest-showcase-medical` and the
//! standalone `sentinel`): the device seam and its mock, the performance
//! contract, the reference panel, and the device scenarios.

pub mod contract;
pub mod device;
pub mod panel;
pub mod scenarios;
